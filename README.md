# Улучшенный Telegram Downloader

## Описание
Модульный и масштабируемый Telegram Downloader с расширенными возможностями для скачивания, фильтрации, валидации и экспорта сообщений из Telegram.

## Особенности

### 🔧 Модульная архитектура
- **Разделение ответственности** между модулями
- **Гибкая конфигурация** через YAML файл
- **Расширяемость** без изменения основного кода

### 📊 Улучшенные возможности
- ✅ **Возобновление прерванного скачивания**
- ✅ **Фильтрация по типам контента**
- ✅ **Валидация и очистка данных**
- ✅ **Экспорт в различные форматы** (JSON, JSONL, CSV, Excel, HTML, XML, Markdown)
- ✅ **Скачивание медиафайлов** (фото, видео, аудио, документы, голосовые сообщения, стикеры, контакты, локации)
- ✅ **Потоковая обработка** для экономии памяти
- ✅ **Интерактивный режим** с выбором чатов
- ✅ **Улучшенная обработка ошибок** и логирование
- ✅ **Поиск чатов по названию**
- ✅ **Удаление дубликатов медиафайлов**
- ✅ **Параллельное скачивание медиафайлов**
- ✅ **Сбор статистики по скачанным медиафайлам**
- ✅ **Автоматическое создание HTML-отчетов с встроенными медиа**
- ✅ **Разделение больших экспорта на части**
- ✅ **Сжатие старых экспортных файлов**
- ✅ **Мониторинг использования памяти**
- ✅ **Фильтрация по диапазону дат**
- ✅ **Фильтрация по ID отправителей**
- ✅ **Фильтрация по наличию ссылок и упоминаний**
- ✅ **Фильтрация по пересланным и отредактированным сообщениям**
- ✅ **Валидация обязательных полей**
- ✅ **Очистка HTML-сущностей и лишних пробелов**
- ✅ **Удаление дубликатов сообщений**
- ✅ **Генерация отчетов о валидации**

## Установка

### Требования
```bash
pip install -r requirements.txt
```

### Настройка
1. Скопируйте `config.yaml.example` в `config.yaml`
2. Настройте параметры в `config.yaml`
3. Убедитесь, что `auth_info.py` содержит корректные данные для подключения

## Использование

### Быстрый старт
```bash
# Показать список чатов
python telegram_downloader.py --list

# Скачать все сообщения из чата
python telegram_downloader.py --chat-id 123456789

# Скачать сообщения за последние 7 дней
python telegram_downloader.py --chat-id 123456789 --days 7

# Скачать с медиафайлами
python telegram_downloader.py --chat-id 123456789 --media

# Экспорт в несколько форматов
python telegram_downloader.py --chat-id 123456789 --formats json,csv,excel

# Интерактивный режим
python telegram_downloader.py --interactive
```

### Конфигурация

#### Основные параметры (`config.yaml`)
```yaml
# Настройки скачивания
batch_size: 1000
max_concurrent_media: 3
resume_downloads: true

# Директории
export_dir: "exports"
media_dir: "downloads/media"

# Форматы экспорта по умолчанию
export_formats:
  - json
  - csv

# Фильтры
filters:
  content_types:
    - text
    - photo
    - video
  exclude_patterns:
    - "spam"
    - "advertisement"
  min_length: 1
  max_length: 10000

# Валидация
validation:
  required_fields:
    - id
    - date
    - message
  max_file_size: 52428800  # 50MB
  allowed_media_types:
    - image/jpeg
    - image/png
    - video/mp4
```

## Архитектура

### Основные модули

#### 1. `telegram_downloader.py`
Главный модуль, координирующий работу всех компонентов. Реализует:
- Получение списка чатов с детальной информацией
- Поиск чатов по названию
- Интерактивный режим с выбором чатов
- Скачивание сообщений с поддержкой фильтрации, валидации и экспорта
- Управление медиафайлами

#### 2. `resume_manager.py`
Управление возобновлением прерванных загрузок. Реализует:
- Сохранение прогресса скачивания в JSON-файлах
- Восстановление после сбоев
- Управление точками восстановления
- Пометку завершенных скачиваний
- Получение информации о возможности возобновления
- Список незавершенных скачиваний
- Проверку точки возобновления

#### 3. `content_filter.py`
Фильтрация сообщений по различным критериям. Реализует:
- Фильтрацию по типам медиа (текст, фото, видео, аудио, документы, стикеры, голосовые сообщения, заметки, контакты, локации, опросы, игры)
- Фильтрацию по текстовым паттернам (регулярные выражения)
- Фильтрацию по диапазону дат
- Фильтрацию по ID отправителей
- Фильтрацию по минимальной длине текста
- Фильтрацию по наличию ссылок
- Фильтрацию по наличию упоминаний
- Фильтрацию по пересланным сообщениям
- Фильтрацию по отредактированным сообщениям
- Применение всех активных фильтров к сообщению
- Создание конфигурации фильтров из параметров
- Текстовое описание активных фильтров

#### 4. `data_validator.py`
Валидация и очистка данных сообщений. Реализует:
- Проверку обязательных полей (ID, дата, сообщение)
- Проверку формата ID сообщения
- Проверку формата даты
- Проверку длины текста (минимальная и максимальная)
- Проверку удаленных пользователей
- Очистку текста (декодирование HTML-сущностей, удаление лишних пробелов, удаление невидимых символов)
- Нормализацию даты
- Очистку информации об отправителе
- Очистку медиа информации
- Удаление дубликатов сообщений
- Валидацию и очистку пакета сообщений
- Статистику валидации
- Экспорт отчета о валидации

#### 5. `export_manager.py`
Экспорт данных в различные форматы. Поддерживает:
- Экспорт в JSON, JSONL, CSV, Excel, HTML, XML, Markdown
- Автоматическое создание имен файлов с временной меткой
- Плоское представление вложенных структур данных
- Сериализацию сложных объектов (даты и т.д.)
- Генерацию HTML-отчетов с встроенными медиафайлами
- Создание относительных путей к медиафайлам в HTML
- Экспорт в несколько форматов одновременно
- Обработку ошибок при экспорте

#### 6. `media_downloader.py`
Скачивание медиафайлов из сообщений Telegram. Поддерживает:
- Фото, видео, аудио, документы, голосовые сообщения, стикеры, контакты, локации
- Организацию файлов по поддиректориям
- Генерацию безопасных имен файлов на основе ID сообщения
- Определение типа медиа по MIME-типу
- Извлечение метаданных (длительность, размеры, исполнитель и т.д.)
- Параллельное скачивание с ограничением одновременных потоков
- Проверку существующих файлов по хэшу для предотвращения дубликатов
- Сбор статистики по скачанным файлам
- Удаление дубликатов медиафайлов по хэшу

#### 7. `stream_processor.py`
Потоковая обработка для экономии памяти. Реализует:
- Потоковую обработку сообщений с постепенной записью в файл
- Создание генераторов с ограничением памяти
- Разделение больших экспорта на несколько файлов
- Сжатие старых экспортных файлов
- Мониторинг использования памяти
- Принудительный сбор мусора при превышении порога
- Прогресс-бар для отслеживания процесса

## Примеры использования

### Скрипт для массового скачивания
```python
import asyncio
from telegram_downloader import TelegramDownloader

async def batch_download():
    downloader = TelegramDownloader()
    
    chat_ids = [123456789, 987654321, 555555555]
    
    for chat_id in chat_ids:
        result = await downloader.download_chat(
            chat_id=chat_id,
            days_limit=30,
            download_media=True,
            export_formats=['json', 'csv', 'excel'],
            resume=True
        )
        print(f"Чат {chat_id}: {result['valid_messages']} сообщений")

if __name__ == "__main__":
    asyncio.run(batch_download())
```

### Кастомная фильтрация
```python
from content_filter import ContentFilter

filters = {
    'content_types': ['text', 'photo'],
    'date_range': {
        'start': '2024-01-01',
        'end': '2024-12-31'
    },
    'keywords': ['python', 'programming'],
    'exclude_patterns': ['spam', 'bot']
}

filter = ContentFilter(filters)
```

## Логирование

Логи сохраняются в:
- `telegram_downloader.log` - основной лог
- `exports/` - экспортированные данные
- `downloads/media/` - скачанные медиафайлы

## Разработка

### Добавление нового формата экспорта
1. Добавьте метод в `export_manager.py`
2. Обновите `export_multiple_formats`
3. Добавьте в конфигурацию

### Добавление нового фильтра
1. Добавьте метод в `content_filter.py`
2. Обновите `apply_filters`
3. Добавьте параметр в конфигурацию

## Поддержка

При возникновении проблем:
1. Проверьте `telegram_downloader.log`
2. Убедитесь в корректности `auth_info.py`
3. Проверьте права доступа к директориям

## Лицензия
MIT License